import { JobFrequencePipe } from './job-frequence.pipe';

describe('JobFrequencePipe', () => {
  it('create an instance', () => {
    const pipe = new JobFrequencePipe();
    expect(pipe).toBeTruthy();
  });
});
